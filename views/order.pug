html
    head
    title Signup
    body
        if(message)
            h4 #{message}
        form(action = "/order" method = "POST")
            input(name = "userid" type = "number" required placeholder = "User ID")
            input(name = "productid" type = "number" required placeholder = "Email")
            button(type = "Submit") Sign me up!